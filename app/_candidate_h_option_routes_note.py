# This file intentionally exists only as a deployment marker for Candidate H option OOS routing.
# The actual route is registered directly in app.main so Render deployments using app.main:app expose it.
